server:
  port: 8088

spring:
  cloud:
    stream:
      default:
        producer:
          headerMode: raw
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        leads-out:
          destination: topic-lead
          contentType: application/json
